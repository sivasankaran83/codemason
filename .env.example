# Template for `.env` (which is gitignored). Copy to `.env` and fill in.
#
# codemason does not parse this file — load it into your environment first,
# or pass --base-url / --api-key explicitly. See README.md.

CODEMASON_BASE_URL=https://openrouter.ai/api/v1
CODEMASON_API_KEY=

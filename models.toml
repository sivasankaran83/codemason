# Operator allowlist, in preference order. The first [[model]] entry is the
# default used when `codemason run` is given no --model flag.
#
# These ids were taken from the live OpenRouter catalogue and validated with
# `codemason models --check` (all three pass the gate: native tool-calling
# support, context length above the minimum below, not a router pseudo-model).
# Re-run that check before trusting this file — provider catalogues change,
# ids get deprecated, and a stale id fails at the gate rather than silently
# falling back.
#
# Prices below are USD per million tokens (input/output) at the time of
# validation, recorded to make the cost ordering reviewable — the runner
# never estimates cost, it only reports what the provider returns.

# Primary. MiniMax's M2 line is agentic/coding-tuned; m2.7 is the newest and
# cheapest of that line. Note that not every MiniMax id is usable here —
# `minimax-01` and `minimax-m2-her` do not advertise tool support and are
# refused at the gate, correctly.
[[model]]
id = "minimax/minimax-m2.7"            # $0.24 in / $0.96 out
role = "primary"

# Fallback, and a different vendor to the primary — which is the point, since
# vendor independence is what M1 exists to prove. Cheapest output of the set.
#
# NOTE: role is operator metadata. M1 does NOT switch models automatically on
# failure; `codemason run` uses `--model`, or the first entry when none is
# given. Falling back is the supervisor's job — re-dispatch with `--model`
# naming this id.
[[model]]
id = "deepseek/deepseek-v3.2"          # $0.26 in / $0.38 out
role = "fallback"

# OpenAI's coding-tuned small model. Third vendor.
[[model]]
id = "openai/gpt-5.1-codex-mini"       # $0.25 in / $2.00 out
role = "fallback"

# Known-good in this repository: this is the model WP3's live AC7 run used.
# It works, with one recorded quirk — it reached for `list_files` before
# `context_search` when asked to describe repository structure.
[[model]]
id = "openai/gpt-4o-mini"              # $0.15 in / $0.60 out
role = "fallback"

# --- supervisor planners -------------------------------------------------
#
# The planner and the jobs are separate choices on purpose. Every failure
# observed in testing came from judgement -- task text that sent a job
# exploring instead of writing, or a diagnosis that named the wrong cause --
# never from the mechanical parts. So spend on judgement and economise on
# execution.
#
# It costs less than it sounds. Planning runs once per build; the jobs run
# once per item per cycle. A Sonnet plan is roughly the price of one job.

[[model]]
id = "anthropic/claude-sonnet-5"        # $2.00 in / $10.00 out, 1M context
role = "planner"

# For a resumed build, where the previous plan did not survive verification
# and the new one is written against the memory of why. The hardest judgement
# in the loop and the rarest call in the build.
[[model]]
id = "anthropic/claude-opus-5"          # $5.00 in / $25.00 out, 1M context
role = "planner-hard"

[gating]
min_context_length = 32000
require_tool_support = true
allow_unlisted = false
# Extends the built-in router/auto-select denylist (exact id
# "openrouter/auto", ids ending "/auto", ids containing "auto-router") with
# extra substrings to reject, for providers with their own routing
# conventions.
deny_id_patterns = []
